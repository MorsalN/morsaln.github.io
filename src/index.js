import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import './index.css';

// Rendering the App component App.jsx inside the div #root
ReactDOM.render(<App />, document.getElementById('root'));
